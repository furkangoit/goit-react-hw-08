import SearchBox from "../../SearchBox/SearchBox";

const Filter = () => {
    return (
        <div>
            <h2>Filter Contacts</h2>
            <SearchBox />
        </div>
    );
};

export default Filter;